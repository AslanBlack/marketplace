<%= render 'layouts/navigation' %>
<h1 align="center">Nos membres</h1>

<%= will_paginate %>

<ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= link_to user.username.titleize, user %>
      <% if current_user.admin? && !current_user?(user) %>
       | <%= link_to "Supprimer le compte", user, method: :delete, 
                                           data: { confirm: "Est-vous sure ?
                                           Attention, cette action est irréversible." } %>
          <% end %>  
    </li>
  <% end %>
</ul>
<%= will_paginate %>

